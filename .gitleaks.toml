title = "multideck gitleaks config"

[extend]
useDefault = true

[[rules]]
id = "tailscale-magdns-hostname"
description = "Tailscale MagicDNS hostname (contains the tailnet name)"
regex = '''[a-z0-9-]+\.tail[a-z0-9-]+\.ts\.net'''
tags = ["tailscale", "infra"]
