# Demo media

Drop the hero recording here as `demo.gif`. It is referenced by the `<!-- DEMO -->`
placeholder near the top of the repo [README](../../README.md); until the GIF exists,
the ASCII multi-monitor diagram there is the visual stand-in (no broken `<img>` link).

**What to record:** `magent --go` fanning a terminal out per project across the
monitors, auto-tiling them into the column x row grid, and an attention badge flipping
(e.g. `magent: api` -> `magent: [!] api` when an agent needs input).

**Target:** ~800px wide, under 10MB (GitHub inlines GIFs up to 10MB). Keep it short
(~10-20s) and loop-friendly.
